"""Cross-implementation task contracts.

Only trainer or pipeline logic proven reusable by more than one concrete
implementation belongs here.  Method-specific execution stays beside the
method in ``trainers`` and ``pipelines``.
"""
