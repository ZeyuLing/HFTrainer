═════════════════════════════════════════════════════════════════════════════════
COMPREHENSIVE SHAPE ANALYSIS: text_vec_raw (vtxt sentence embedding) Pipeline
═════════════════════════════════════════════════════════════════════════════════

EXECUTIVE SUMMARY
─────────────────────────────────────────────────────────────────────────────────
✓ All shapes are CORRECTLY COMPATIBLE
✓ No shape mismatch exists when concatenating null_vtxt and vtxt_input for CFG
✓ The ORIGINAL code (f56a87a) had a different bug: CFG null TEXT MASK mismatch, not shape
✓ The 2026-05-15 fix corrected the null text attention mask, not the vtxt shape


═════════════════════════════════════════════════════════════════════════════════
1. null_vtxt_feat DEFINITION
═════════════════════════════════════════════════════════════════════════════════

File: hftrainer/models/motion/hymotion_m2m/bundle.py, line 212
Code: self.null_vtxt_feat = nn.Parameter(torch.randn(1, 1, 768) * 0.01)
      
Shape at definition:  (1, 1, 768)
                       ↑  ↑  ↑
                       |  |  └─ 768-dim sentence embedding (vtxt_input_dim)
                       |  └──── 1 sequence position (sentence-level, not token-level)
                       └─────── 1 in batch dimension (broadcasted/expanded later)


═════════════════════════════════════════════════════════════════════════════════
2. text_vec_raw SOURCE (Text Encoder Output)
═════════════════════════════════════════════════════════════════════════════════

File: hftrainer/models/motion/hymotion_m2m/network/text_encoder.py, line 254
Code: vtxt_raw = nn.functional.normalize(sentence_embeddings, p=2, dim=1).unsqueeze(1)
      
Shape: (B, 1, 768)
        ↑  ↑  ↑
        |  |  └─ Same 768-dim encoding
        |  └──── unsqueeze(1) adds sequence dimension → (B, 768) → (B, 1, 768)
        └─────── Batch size from input

Entry point: HYTextModel.encode() at line 197 returns:
  return vtxt_raw, ctxt_raw, ctxt_length
where vtxt_raw shape is (B, 1, 768) for batch size B


═════════════════════════════════════════════════════════════════════════════════
3. TRAINER PATH: Loading text_vec_raw
═════════════════════════════════════════════════════════════════════════════════

File: hftrainer/trainers/motion/hymotion_m2m_trainer.py, lines 138-142

Code:
  if batch.get('text_vec_raw') is not None:
      vtxt_input = batch['text_vec_raw'].to(device)

Input:  batch['text_vec_raw'] from dataset.batch_keys
        Shape: (B, 1, 768) — directly from text encoder output

Output: vtxt_input = (B, 1, 768)


PROCESSING THROUGH mask_text_cond (Line 180)
─────────────────────────────────────────────
File: hftrainer/models/motion/hymotion_m2m/bundle.py, line 315-376

Function signature:
  def mask_text_cond(self, vtxt: Tensor, ctxt: Tensor, ...) -> Tuple[Tensor, Tensor]
  
Input vtxt:   (B, 1, 768)  ← From trainer line 142
Processing:  
  - Bernoulli random mask applied per-sample → some samples replaced with null
  - null_vtxt_feat.expand_as(vtxt) → (B, 1, 768)
  - torch.where() replaces: new_vtxt = where(mask, null_vtxt, vtxt)
  
Output vtxt:  (B, 1, 768)  ← Shape preserved


═════════════════════════════════════════════════════════════════════════════════
4. PIPELINE PATH: Text Branch (text_vec_raw present)
═════════════════════════════════════════════════════════════════════════════════

File: hftrainer/pipelines/motion/hymotion_m2m_pipeline.py, line 191-196

Code:
  if 'text_vec_raw' in batch:
      vtxt_input = batch['text_vec_raw'].to(device=device, dtype=model_dtype)

Input:  batch['text_vec_raw']
        Shape: (B, 1, 768)

Output: vtxt_input = (B, 1, 768)


═════════════════════════════════════════════════════════════════════════════════
5. PIPELINE PATH: No-text Branch (text_vec_raw absent)
═════════════════════════════════════════════════════════════════════════════════

File: hftrainer/pipelines/motion/hymotion_m2m_pipeline.py, line 209

Code:
  vtxt_input = self.bundle.null_vtxt_feat.to(dtype=model_dtype).expand(B, 1, -1)
  
Start:  null_vtxt_feat shape: (1, 1, 768)
Expand: .expand(B, 1, -1)
Result: vtxt_input = (B, 1, 768)  ✓

Note: expand() does NOT create new memory, just views with B repeated


═════════════════════════════════════════════════════════════════════════════════
6. CFG NULL BRANCH EXPANSION (Line 230)
═════════════════════════════════════════════════════════════════════════════════

File: hftrainer/pipelines/motion/hymotion_m2m_pipeline.py, line 230

Code:
  if do_cfg:
      null_vtxt = self.bundle.null_vtxt_feat.to(dtype=model_dtype).expand_as(vtxt_input)

Input:  self.bundle.null_vtxt_feat
        Shape: (1, 1, 768)
        
        vtxt_input (from either branch)
        Shape: (B, 1, 768)
        
Output: null_vtxt = expand_as(vtxt_input)
        Shape: (B, 1, 768)  ✓

CRITICAL: expand_as() takes the SHAPE of vtxt_input and expands to match
Since vtxt_input is (B, 1, 768), null_vtxt becomes (B, 1, 768)


═════════════════════════════════════════════════════════════════════════════════
7. CFG CONCATENATION (Lines 259-261)
═════════════════════════════════════════════════════════════════════════════════

File: hftrainer/pipelines/motion/hymotion_m2m_pipeline.py, line 259-261

Code:
  x_pred = self.bundle.predict_flow(
      ...
      vtxt_input=(
          vtxt_input if not do_cfg
          else torch.cat([null_vtxt, vtxt_input], dim=0)
      ),
      ...
  )

CFG Branch:
  torch.cat([null_vtxt, vtxt_input], dim=0)
  
  Inputs:
    null_vtxt:    (B, 1, 768)   ← From line 230
    vtxt_input:   (B, 1, 768)   ← From either text or no-text branch
  
  Concatenation along dim=0:
    Result: (2B, 1, 768)  ✓ CORRECT
    
  Layout:
    [0:B]       = null branch embeddings
    [B:2B]      = conditioned branch embeddings


═════════════════════════════════════════════════════════════════════════════════
8. TRANSFORMER INPUT SIGNATURE
═════════════════════════════════════════════════════════════════════════════════

File: hftrainer/models/motion/hymotion_m2m/bundle.py, line 486-501

Docstring:
  Args:
      vtxt_input: sentence-level text embeddings, (B, 1, Dv).
      
Code:
  def predict_flow(
      self,
      x_input: Tensor,
      ctxt_input: Tensor,
      vtxt_input: Tensor,    ← (B, 1, Dv) or (2B, 1, Dv) for CFG
      timesteps: Tensor,
      ...
  ) -> Tensor:
      return self.motion_transformer(
          ...
          vtxt_input=vtxt_input,
          ...
      )

Expected shape: (B, 1, Dv) where Dv=768
For CFG: (2B, 1, 768) — first B samples are null, next B are conditioned


═════════════════════════════════════════════════════════════════════════════════
9. SHAPE COMPATIBILITY MATRIX
═════════════════════════════════════════════════════════════════════════════════

┌─────────────────────────┬──────────────┬────────────────────────────────┐
│ Location                │ Shape        │ Status                         │
├─────────────────────────┼──────────────┼────────────────────────────────┤
│ null_vtxt_feat (init)   │ (1, 1, 768)  │ ✓ Definition                   │
│ bundle.encode_text()    │ (B, 1, 768)  │ ✓ Text encoder output          │
│ batch['text_vec_raw']   │ (B, 1, 768)  │ ✓ Matches encoder              │
│ vtxt_input (trainer)    │ (B, 1, 768)  │ ✓ Passes through trainer       │
│ vtxt_input (pipeline)   │ (B, 1, 768)  │ ✓ From batch or null-expand    │
│ null_vtxt (CFG)         │ (B, 1, 768)  │ ✓ expand_as matches            │
│ torch.cat result        │ (2B, 1, 768) │ ✓ Concatenation along dim=0    │
│ predict_flow input      │ (2B, 1, 768) │ ✓ Matches signature            │
└─────────────────────────┴──────────────┴────────────────────────────────┘


═════════════════════════════════════════════════════════════════════════════════
10. COMPARISON: ORIGINAL (f56a87a) vs CURRENT
═════════════════════════════════════════════════════════════════════════════════

ORIGINAL CODE (f56a87a):
─────────────────────────
Line 254-258:
  ctxt_tokens = ctxt_input.shape[1]
  null_vtxt = self.bundle.null_vtxt_feat.to(dtype=model_dtype).expand(B, 1, -1)
  null_ctxt = self.bundle.null_ctxt_input.to(dtype=model_dtype).expand(B, ctxt_tokens, -1).contiguous()
  null_ctxt_mask = ctxt_mask_temporal  # SAME attention coverage ← BUG

Line 278-283:
  torch.cat([null_vtxt, vtxt_input], dim=0)  ← Correct shape (B,1,768) + (B,1,768)

THE BUG (2026-05-15 found and fixed):
  The mask = ctxt_mask_temporal line reused the original caption's attention mask.
  Training code (mask_text_cond) uses a different pattern:
    - When text is dropped, BOTH vtxt AND ctxt are replaced with nulls
    - AND the ctxt_mask_temporal is updated to attend only to position 0
  But the ORIGINAL pipeline code kept the original mask (128 positions for captioned, 1 for uncond)
  This caused train-inference MISMATCH for the null branch


CURRENT CODE (Fixed 2026-05-15):
────────────────────────────────
Line 229-235:
  if do_cfg:
      null_vtxt = self.bundle.null_vtxt_feat.to(dtype=model_dtype).expand_as(vtxt_input)
      null_ctxt = self.bundle.null_ctxt_input.to(dtype=model_dtype).expand(
          ctxt_input.shape[0], ctxt_input.shape[1], -1
      ).contiguous()
      # Build attention mask for the null branch that matches training
      null_ctxt_mask = torch.zeros_like(ctxt_mask_temporal)
      null_ctxt_mask[:, 0] = True  # Only position 0 is valid ← FIXED

Changes:
  1. null_vtxt uses expand_as() instead of fixed expand(B, 1, -1)
     → More explicit about matching vtxt_input shape
  2. null_ctxt_mask is newly created with only position 0 valid
     → Matches training convention from mask_text_cond()


═════════════════════════════════════════════════════════════════════════════════
11. WHY NO SHAPE MISMATCH IN CONCATENATION
═════════════════════════════════════════════════════════════════════════════════

Key observation: torch.cat requires all dimensions EXCEPT concatenation axis to match

For: torch.cat([null_vtxt, vtxt_input], dim=0)

                 null_vtxt:  (B, 1, 768)
                vtxt_input:  (B, 1, 768)
    ┌────────────────────────────────────┐
    │ Dimension comparison:               │
    ├────────────────────────────────────┤
    │ dim=0 (concat axis):   B, B ← OK   │
    │ dim=1:                 1, 1 ← OK   │
    │ dim=2:                768,768← OK  │
    └────────────────────────────────────┘
    
Result: (B+B, 1, 768) = (2B, 1, 768) ✓


═════════════════════════════════════════════════════════════════════════════════
12. FINAL ANSWER: Exact Shapes in Both Branches
═════════════════════════════════════════════════════════════════════════════════

TEXT PRESENT BRANCH:
────────────────────
  batch['text_vec_raw']:        (B, 1, 768)
  vtxt_input in trainer:        (B, 1, 768)
  after mask_text_cond():       (B, 1, 768)
  vtxt_input in pipeline:       (B, 1, 768)
  null_vtxt (CFG):              (B, 1, 768) ← via expand_as
  torch.cat result:             (2B, 1, 768) ← CORRECT

TEXT ABSENT BRANCH:
────────────────────
  batch['text_vec_raw']:        None (not present)
  vtxt_input in pipeline:       (B, 1, 768) ← via expand(B, 1, -1)
  null_vtxt (CFG):              (B, 1, 768) ← via expand_as
  torch.cat result:             (2B, 1, 768) ← CORRECT


═════════════════════════════════════════════════════════════════════════════════
CONCLUSION
═════════════════════════════════════════════════════════════════════════════════

✓ NO SHAPE MISMATCH when doing torch.cat([null_vtxt, vtxt_input], dim=0)
✓ Both branches produce (B, 1, 768) for vtxt_input independently
✓ null_vtxt correctly expands from (1, 1, 768) to (B, 1, 768) via expand_as()
✓ CFG concatenation produces (2B, 1, 768) which is transformer-compatible

THE ACTUAL BUG (2026-05-15):
  Not shape incompatibility, but CFG null text MASK mismatch:
  - ORIGINAL: reused caption's attention mask (128 positions) for null branch
  - CURRENT: uses position-0-only mask to match training distribution
  
When text IS present:   vtxt_input = (B, 1, 768) — no issue
When text is NOT:       vtxt_input = (B, 1, 768) via null-expand — no issue
When doing CFG:         torch.cat produces (2B, 1, 768) — no shape issue

All shapes are COMPATIBLE and CORRECT in current code.
