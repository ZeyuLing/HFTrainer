╔════════════════════════════════════════════════════════════════════════════════════════════════════════════╗
║                         COMPLETE MOTION JSON GENERATION PIPELINE                                          ║
╚════════════════════════════════════════════════════════════════════════════════════════════════════════════╝

STAGE 1: TEXT-TO-MOTION GENERATION (HyMotion T2M)
┌─────────────────────────────────────┐
│ HyMotion T2M 1.0-Lite Inference     │
│ Input: Text prompt + duration       │
│ Output: motion_201 (T, 201)         │
│ • [0:3] translation                 │
│ • [3:135] 22x rot6d (row-major)     │
│ • [135:201] auxiliary channels      │
└─────────────────────────────────────┘
            ↓
            Extract first 135 dims
            ↓
┌─────────────────────────────────────┐
│ motion_135 NPZ File                 │
│ Keys: motion_135 (T, 135), fps      │
│ Location: output/.../data/npz/      │
└─────────────────────────────────────┘

STAGE 2: MOTION-TO-ROBOT RETARGETING (PyRoki V6)
            ↓
[pipeline_motion_to_robot.py]
│  Step 2a: motion_135 → PyRoki keypoints (.npy)
│           • SMPL FK to get joint positions
│           • Geometric surgery (G1-specific)
│  
│  Step 2b: Extract foot contact labels (.npz)
│           • Left/right foot contact prediction
│  
│  Step 2c: PyRoki retargeting (keypoints → robot NPZ)
│           • jaxls LeastSquaresProblem optimizer (800 iter)
│           • Output: base_frame_pos, base_frame_wxyz, joint_angles
│
└─ Retargeted NPZ (T, 3+4+N_joints)
            ↓

STAGE 3: TORCH CACHE GENERATION (ProtoMotions)
┌──────────────────────────────────────────────────────────────┐
│ convert_pyroki_retargeted_robot_motions_to_proto.py           │
│                                                                │
│ Input: base_frame_pos, base_frame_wxyz, joint_angles          │
│                                                                │
│ Processing:                                                    │
│  1. Reconstruct qpos from base_frame + joint_angles           │
│  2. FK to get rigid_body_pos[t, :, 3]                         │
│  3. Compute cartesian velocities                              │
│  4. 🔴 APPLY HEIGHT FIX (line 352-362):                       │
│     • fix_height_per_frame(offset=0.02)                       │
│     • fix_height(offset=0.04) ← Additional fix                │
│     • Updates velocities accordingly                          │
│  5. Contact labels from PyRoki output                         │
│                                                                │
│ Output: torch.save(motion.to_dict(), ".motion")               │
│         Keys: rigid_body_pos, rigid_body_rot, dof_pos,        │
│               dof_vel, fps, rigid_body_contacts               │
└──────────────────────────────────────────────────────────────┘
            ↓
┌─────────────────────────────────────┐
│ .motion Torch Pickle File           │
│ (height-CORRECTED)                  │
│ Location: retarget_dir/ or cache/   │
└─────────────────────────────────────┘

STAGE 4: JSON CONVERSION FOR WEB (🔑 THE KEY STEP)
            ↓
┌─────────────────────────────────────────────────────────────────┐
│ convert_cache_to_json.py                                         │
│ (scripts/embodied/convert_cache_to_json.py)                      │
│                                                                   │
│ Function: convert_cache_to_json(cache_path, output_path)        │
│                                                                   │
│ Input: torch .motion file                                        │
│                                                                   │
│ Processing:                                                      │
│  1. torch.load(cache_path, weights_only=False)                  │
│  2. Extract:                                                     │
│     • rigid_body_pos → used as root_pos (from body 0/pelvis)    │
│     • rigid_body_rot → used as root_quat (from body 0)          │
│     • dof_pos → used as dof_pos (29 joints)                     │
│     • fps from motion_dt or fps key                             │
│  3. Build frame objects:                                         │
│     frame = {                                                    │
│       "root_pos": [x, y, z],         ← Z is ALREADY corrected  │
│       "root_quat": [x, y, z, w],                               │
│       "dof_pos": [v0, v1, ..., v28]                            │
│     }                                                            │
│  4. json.dump(result, f, separators=(",", ":"))  ← Compact     │
│                                                                   │
│ Output: JSON with structure {                                    │
│   "fps": 50,                                                     │
│   "num_frames": N,                                               │
│   "joint_names": [...29 names...],                              │
│   "root_body_index": 0,                                          │
│   "frames": [{...}, {...}, ...]                                 │
│ }                                                                │
└─────────────────────────────────────────────────────────────────┘
            ↓
┌────────────────────────────────────────┐
│ .json Motion File (FINAL OUTPUT)       │
│ Location: output/.../data/motions/     │
│ Example: v4_walk_015.json              │
│ Size: ~100KB-500KB per motion          │
│ Content: 29 joint angles + root pose   │
│          for each frame                │
└────────────────────────────────────────┘

═══════════════════════════════════════════════════════════════════════════════════════════════════════════════

KEY INSIGHTS:

1️⃣  HEIGHT FIX TIMING:
   ✓ Applied in Stage 3 (convert_pyroki_retargeted_robot_motions_to_proto.py)
   ✓ BEFORE torch.save() → torch .motion file already contains corrected Z
   ✓ JSON inherits these corrected values (no re-adjustment in Stage 4)

2️⃣  NEGATIVE Z VALUES:
   → Not "negative" in absolute sense, but relative to fixed pelvis height
   → Result of fix_height_per_frame() setting baseline at ~0.02m above ground
   → Intermediate dips in trajectory now appear as negative deltas

3️⃣  TWO CALLING PATHWAYS FOR STAGE 4:

   Pathway A (batch_pipeline_to_web.py):
   ─────────────────────────────────────
   for npz_file in npz_files:
       cache = run_pipeline(npz_file)        ← Stages 1-3
       json = convert_cache_to_json(cache)   ← Stage 4 (line 226)

   Pathway B (batch_t2m_to_embodied.py):
   ─────────────────────────────────────
   for prompt in prompts:
       npz = run_t2m_inference(prompt)       ← Stage 1
       cache = run_retarget_pipeline(npz)    ← Stages 2-3
       json = convert_cache_to_json(cache)   ← Stage 4 (line 845)

4️⃣  FILE EXTENSIONS:
   ✓ .motion (torch pickle) — binary, not JSON
   ✓ .motion (json) — also .motion extension but JSON content
   → Naming is confusing! Both use .motion extension

5️⃣  NO BYPASS OF HEIGHT FIX:
   ✗ There is NO pathway that creates JSON without height fix
   ✓ All JSON files use the height-corrected torch .motion cache

═══════════════════════════════════════════════════════════════════════════════════════════════════════════════

TECHNICAL DETAILS:

Torch .motion Dict Keys:
  • rigid_body_pos: (T, N_bodies, 3) ← shape [time, body, xyz]
  • rigid_body_rot: (T, N_bodies, 4) ← shape [time, body, xyzw]
  • dof_pos: (T, 29)                 ← 29 joint angles in [-π, π]
  • dof_vel: (T, 29)                 ← joint velocities
  • rigid_body_vel: (T, N_bodies, 3) ← body velocities (velocity channel)
  • rigid_body_contacts: (T, N_bodies) ← foot contact bool flags
  • fps: int                         ← frames per second
  • motion_dt: float                 ← 1/fps (inverse)

JSON Frame Object (per time step):
  {
    "root_pos": [0.431, 0.094, 0.749],         ← pelvis position
    "root_quat": [0.149, -0.102, 0.046, 0.982], ← pelvis rotation [x,y,z,w]
    "dof_pos": [-1.078, 0.106, ..., -0.065]   ← 29 joint angles
  }

═══════════════════════════════════════════════════════════════════════════════════════════════════════════════
